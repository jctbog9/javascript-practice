const addValueToNumber = (inputNumber = 5, amountToAdd = 3) => {
    return inputNumber + amountToAdd
}

// addValueToNumber(117, 13)
// addValueToNumber(72, 1)
// addValueToNumber(113)
console.log(addValueToNumber(113, 7))

console.log('log whatever is inside this set of ()')